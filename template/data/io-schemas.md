# Input / Output Schemas

---
type: io-schemas
scope: project
updated: [YYMMDD-HHMMSS]
---

## Purpose

Navigation index. Full field-by-field definitions live in each package's `io-schema.md`. Read this file to find the right per-package file; read that file for the actual types and signatures.

---

## Package index

| Layer | Package | npm name | What it exports | Schema file |
|---|---|---|---|---|
| [layer] | [package-name] | [full package name] | [summary of exports] | `data/packages/[package]/io-schema.md` |

<uncertain class="stub">Source had 14 populated rows. Emptied to one placeholder — genuine project inventory, not shape.</uncertain>
